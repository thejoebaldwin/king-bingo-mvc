
@{
    var user = @ViewBag.User;
    ViewBag.Title = user.Name;
}
@Html.Partial("ProfilePartial", ViewData)
Badges<br />
@foreach (var b in user.Badges)
{
    <img src="@Url.Content(b.ImagePath)" alt="b.Name" />
}
     


